

## hur vi använder git

git add .
git commit -m"mitt commit"
git push origin main


## hur vi gör med bilder

whit api.create_session() as session
    slide =SlideInput(code="filnamne", suffix"A", study_id=78)
    scan =ScanInput(slide_id=slide.id)
    scan_id = scan.id
    image = ImageInput(uuid=uuid4(), path="aasd", scan_id=scan_id) 
    